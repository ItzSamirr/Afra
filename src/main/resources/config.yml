prefix: "&b[&3&lAfra&b] &r"
bypass: true
flag:
  msg:
    enabled: true
    text: "{prefix}&3{player} &bfailed &3{check} &b(&3{type}&b)"
    hover:
      enabled: true
      info-format: "&3{key}: &b{value}"
      text:
        - "&3{player} &b| &3Ping: &b{ping}"
        - " "
        - " &3Description:"
        - "&b{desc}"
        - " "
        - " &3Info:"
        - "&b{info}"
checks:
  movement:
    speed:
      a:
        bypass: true
        enabled: true
        cancel-type: NOTHING
        buffer:
          deaccumulator: 1.0
          accumulator: 1.0
          max: 2.0
    nofall:
      a:
        bypass: false
        enabled: true
        buffer:
          deaccumulator: 1.0
          accumulator: 1.0
          max: 2.0
    jump:
      #
      # Experimental - Unstable!
      #
      a:
        bypass: true
        enabled: true
        cancel-type: "PULLDOWN"
        buffer:
          deaccumulator: 1.0
          accumulator: 1.0
          max: 1.0